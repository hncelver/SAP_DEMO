_schema-version: "3.2"
ID: spacemyfioriapp
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: spacemyfioriapp-destination-content
  type: com.sap.application.content
  requires:
  - name: spacemyfioriapp-destination-service
    parameters:
      content-target: true
  - name: spacemyfioriapp-repo-host
    parameters:
      service-key:
        name: spacemyfioriapp-repo-host-key
  - name: spacemyfioriapp-uaa
    parameters:
      service-key:
        name: spacemyfioriapp-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: spacemyfioriapp_html_repo_host
          ServiceInstanceName: spacemyfioriapp-html5-service
          ServiceKeyName: spacemyfioriapp-repo-host-key
          sap.cloud.service: spacemyfioriapp
        - Authentication: OAuth2UserTokenExchange
          Name: spacemyfioriapp_uaa
          ServiceInstanceName: spacemyfioriapp-xsuaa-service
          ServiceKeyName: spacemyfioriapp-uaa-key
          sap.cloud.service: spacemyfioriapp
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: spacemyfioriapp-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: spacemyfioriapp-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - spacemyfioriapp.zip
      name: spacemyfioriapp
      target-path: resources/
- name: spacemyfioriapp
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: spacemyfioriapp-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: spacemyfioriapp-destination-service
    service-plan: lite
- name: spacemyfioriapp-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: spacemyfioriapp-xsuaa-service
    service-plan: application
- name: spacemyfioriapp-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: spacemyfioriapp-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
